# Database Configuration
DB_PATH=./data/secure_notes.db
DB_ENCRYPTION_KEY=your-very-strong-32-char-key-here-change-this

# Application Encryption Key (32 bytes for AES-256)
APP_ENCRYPTION_KEY=another-32-char-key-for-field-encryption

# Backup Configuration
BACKUP_DIR=./backups
BACKUP_ENCRYPTION_KEY=backup-encryption-key-32-characters
BACKUP_INTERVAL_HOURS=24
BACKUP_RETENTION_DAYS=30

# Audit Log Configuration
AUDIT_LOG_PATH=./logs/audit.log
AUDIT_ASYNC_MODE=true

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_SECOND=10
RATE_LIMIT_BURST=20

# Application Settings
APP_ENV=production
LOG_LEVEL=info